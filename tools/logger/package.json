{
  "name": "@portfolio/logger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun x tsc -p tsconfig.json",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "winston": "^3.17.0",
    "nest-winston": "^1.10.0"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
